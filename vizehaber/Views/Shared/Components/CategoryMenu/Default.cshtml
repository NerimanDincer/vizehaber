@model List<vizehaber.Models.Category>

@foreach (var item in Model)
{
    <a class="collapse-item" asp-controller="Home" asp-action="Index" asp-route-categoryId="@item.Id">
        @item.Name
    </a>
}